/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed the dangerous allowedOrigins: ["*"] setting for security
  experimental: {
    // Only include safe experimental features
  },
  // Add security headers
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff'
          },
          {
            key: 'X-Frame-Options',
            value: 'DENY'
          },
          {
            key: 'X-XSS-Protection',
            value: '1; mode=block'
          }
        ]
      }
    ];
  }
};

module.exports = nextConfig;
